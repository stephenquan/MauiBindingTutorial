<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MauiBindingTutorial.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MauiBindingTutorial"
    x:Name="mainPage">
    <VerticalStackLayout Padding="30" Spacing="25">
        <CollectionView x:Name="collectionView" ItemsSource="{Binding Primes}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Label Text="{Binding .}" />
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <Entry
            x:Name="entry"
            MaxLength="32"
            Text="" />
        <Label x:Name="countLabel" Text="{Binding Text.Length, x:DataType=Entry, Source={Reference entry}}" />
    </VerticalStackLayout>
</ContentPage>
